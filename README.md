Dashboard Cuaca di China
Install library

pip install streamlit
pip install pandas
pip install matplotlib
pip install numpy
pip install streamlit

jalankan enviroment
python -m venv env  
env\Scripts\activate

Run streamlit app
streamlit run dashboard.py